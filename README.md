# My plugin with useful aliases for ZSH

## Requirements

- [ ] Github CLI `gh`
- [ ] git

- `view-run` : View the current Github Action run for the current branch
- `cancel-run` : Cancel the current Github Action run for the current branch

